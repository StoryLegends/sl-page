spring.application.name=slbackend

spring.security.key=${SPRING_SECURITY_KEY:defaultSecretKeyForStoryLegendsProductionAndTesting123456}

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.datasource.hikari.maximum-pool-size=5

# CORS Allowed Origins
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://test.storylegends.xyz,https://test.storylegends.xyz,http://localhost:5173,http://localhost:5174}

# MinIO / S3 (Bucketeer on Heroku sets BUCKETEER_* vars)
minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
minio.access-key=${BUCKETEER_AWS_ACCESS_KEY_ID:${MINIO_ACCESS_KEY:minioadmin}}
minio.secret-key=${BUCKETEER_AWS_SECRET_ACCESS_KEY:${MINIO_SECRET_KEY:minioadmin123}}
minio.bucket-name=${BUCKETEER_BUCKET_NAME:${MINIO_BUCKET_NAME:slbackend-avatars}}
minio.region=${BUCKETEER_AWS_REGION:${MINIO_REGION:}}
minio.public-url=${MINIO_PUBLIC_URL:}
minio.presigned-url-expiry-hours=${MINIO_PRESIGNED_URL_EXPIRY_HOURS:168}
minio.enabled=${MINIO_ENABLED:true}

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# Mailgun
mailgun.api-key=${MAILGUN_API_KEY:}
mailgun.domain=${MAILGUN_DOMAIN:storylegends.xyz}
mailgun.from-email=${MAILGUN_FROM_EMAIL:noreply@storylegends.xyz}
mailgun.from-name=${MAILGUN_FROM_NAME:StoryLegends}
mailgun.base-url=${MAILGUN_BASE_URL:https://api.eu.mailgun.net}

# reCAPTCHA
recaptcha.secret-key=${RECAPTCHA_SECRET_KEY:}
recaptcha.site-key=${RECAPTCHA_SITE_KEY:}
recaptcha.verify-url=https://www.google.com/recaptcha/api/siteverify

# Email Verification
email.verification.expiration=86400000
email.verification.base-url=${FRONTEND_URL:http://localhost:5173}/

# Feature flags
app.recaptcha.enabled=${APP_RECAPTCHA_ENABLED:false}
app.email.enabled=${APP_EMAIL_ENABLED:false}
app.auto-verify-email=${APP_AUTO_VERIFY_EMAIL:true}

# Discord OAuth2
discord.oauth.client-id=${DISCORD_CLIENT_ID:}
discord.oauth.client-secret=${DISCORD_CLIENT_SECRET:}
discord.oauth.redirect-uri=${DISCORD_OAUTH_REDIRECT_URI:${FRONTEND_URL:http://localhost:5173}/auth/discord/callback}

# Discord Bot
discord.bot.enabled=${DISCORD_BOT_ENABLED:false}
discord.bot.token=${DISCORD_BOT_TOKEN:}
discord.guild.id=${DISCORD_GUILD_ID:}
discord.sl-role.id=${DISCORD_SL_ROLE_ID:}

# Logging
logging.level.com.datapeice.slbackend=INFO
logging.level.org.springframework.security=WARN

# Moderation APIs
sightengine.api.user=${SIGHTENGINE_API_USER:}
sightengine.api.secret=${SIGHTENGINE_API_SECRET:}

# Minecraft RCON
minecraft.rcon.enabled=${MINECRAFT_RCON_ENABLED:false}
minecraft.rcon.ip=${MINECRAFT_RCON_IP:localhost}
minecraft.rcon.port=${MINECRAFT_RCON_PORT:25575}
minecraft.rcon.password=${MINECRAFT_RCON_PASSWORD:}

# Anticheat
anticheat.allowed-ips=${ANTICHEAT_ALLOWED_IPS:}
anticheat.api-key=${ANTICHEAT_API_KEY:}
anticheat.retention-days=${ANTICHEAT_RETENTION_DAYS:14}

# Stripe
stripe.secret-key=${STRIPE_SECRET_KEY:sk_test_dummy}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET:whsec_dummy}
app.frontend-url=${FRONTEND_URL:https://test.storylegends.xyz}
frontend.url=${FRONTEND_URL:https://test.storylegends.xyz}
